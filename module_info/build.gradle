apply plugin: 'com.android.library'
apply from: "../lib_build.gradle"

android {
    resourcePrefix "info_" //给 Module 内的资源名增加前缀, 避免资源名冲突
}

dependencies {
    implementation project(':lib_base')
    implementation project(':lib_app')
}